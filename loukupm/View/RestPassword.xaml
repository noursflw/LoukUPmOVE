<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="loukupm.View.RestPassword"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             BackgroundImageSource="baackgroundpage.png"
             xmlns:loc="clr-namespace:loukupm.Langue"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="RestPassword">
    <ScrollView>
        <VerticalStackLayout Margin="25,2,2,24">
            <Grid RowDefinitions="40*,60*">
                <Image Grid.Row="0" Margin="0,100,0,0" WidthRequest="200" HeightRequest="60" Source="slogo.svg"/>
            </Grid>

            <!-- العنوان الرئيسي -->
            <Label Text="{loc:Translate Name=NPasswordOnLogin}" FontFamily="georgia-bold" Margin="0,10,0,0" FontSize="32" TextColor="#D3D3D3" />
            
            <!-- النص الثانوي -->
            <Label Text="{loc:Translate Name=ResetPasswordvv}" FontFamily="georgia-bold" Margin="0,10,0,0" FontSize="16" TextColor="#999999" />
            
            <!-- تسمية البريد -->
            <Label Text="{loc:Translate Name=EMail }" Margin="12,5,0,0" TextColor="White" Padding="10" FontSize="16"/>
            
            <!-- حقل البريد الإلكتروني -->
            <material:TextField 
                x:Name="EmailTextField"
                Text="{Binding Email}" 
                Margin="12,0,30,0" 
                InputBackgroundColor="#444444" 
                CornerRadius="16"
                Icon="email.png"
                HeightRequest="50" 
                Title="Example@gmail.com" />

            <!-- الزر مع مؤشر التحميل -->
            <Grid RowDefinitions="Auto" Margin="12,50,25,25" Padding="0">
                <!-- الزر الرئيسي -->
                <Button 
                    x:Name="SendOtpButton"
                    Grid.Row="0"
                    Clicked="Button_Clicked"
                    TextColor="#1F1B04" 
                    FontSize="22" 
                    FontFamily="georgia-bold"
                    BorderWidth="1" 
                    Text="{loc:Translate Name=linkpass}" 
                    CornerRadius="16"
                    HeightRequest="60"
                    IsEnabled="True" />

                <!-- مؤشر التحميل -->
                <ActivityIndicator 
                    x:Name="LoadingIndicator"
                    Grid.Row="0"
                    IsRunning="False"
                    IsVisible="False"
                    Color="#EBD750"
                    Scale="1.2"
                    VerticalOptions="Center"
                    HorizontalOptions="Center" />
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>