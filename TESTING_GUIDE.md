# 🧪 دليل الاختبار الشامل

## 📱 اختبارات يدوية

### 🔹 اختبار 1: فتح الصفحة وتحميل البيانات

```gherkin
Given: المستخدم في الصفحة الرئيسية
When: ينضغط على "تفاصيل حسابي"
Then: 
  ✅ تحميل بيانات المستخدم
  ✅ عرض اسم المستخدم في TextField
  ✅ عرض صورة المستخدم في Image
  ✅ ActivityIndicator يختفي
  ✅ الأزرار مفعلة
```

**الخطوات:**
1. فتح التطبيق
2. انتقل إلى صفحة تعديل البيانات
3. تحقق من ظهور البيانات الحالية

**النتيجة المتوقعة:** ✅ بيانات المستخدم تظهر صحيحة

---

### 🔹 اختبار 2: تغيير الصورة والمعاينة

```gherkin
Given: صفحة تعديل البيانات مفتوحة
When: ينضغط على "تغيير الصورة"
Then:
  ✅ يفتح معرج الصور
  ✅ اختيار صورة
  ✅ الصورة تظهر فوراً في الـ Avatar
  ✅ عرض رسالة نجاح "تم تحميل الصورة بنجاح"
```

**الخطوات:**
1. اضغط على زر "تغيير الصورة"
2. اختر صورة من المعرج
3. لاحظ الصورة الجديدة تظهر فوراً

**النتيجة المتوقعة:** ✅ الصورة تظهر فوراً بدون الحاجة لحفظ

---

### 🔹 اختبار 3: تحديث الاسم فقط

```gherkin
Given: صفحة تعديل البيانات مفتوحة
When: 
  - تعديل اسم المستخدم
  - عدم تغيير الصورة
  - الضغط على "حفظ التغييرات"
Then:
  ✅ الأزرار تصبح معطلة
  ✅ ActivityIndicator يبدأ
  ✅ الطلب يُرسل (بدون صورة)
  ✅ عند النجاح: "تم تحديث البيانات بنجاح"
  ✅ الأزرار تصبح مفعلة
  ✅ البيانات تُحدث من السيرفر
```

**الخطوات:**
1. عدّل الاسم مثلاً من "أحمد" إلى "محمد"
2. لا تغيّر الصورة
3. اضغط "حفظ التغييرات"
4. انتظر الرد

**النتيجة المتوقعة:** ✅ الاسم يُحفظ بنجاح

---

### 🔹 اختبار 4: تحديث الصورة والاسم معاً

```gherkin
Given: صفحة تعديل البيانات مفتوحة
When: 
  - تعديل اسم المستخدم
  - اختيار صورة جديدة
  - الضغط على "حفظ التغييرات"
Then:
  ✅ الأزرار معطلة
  ✅ ActivityIndicator يعمل
  ✅ رفع الاسم والصورة معاً
  ✅ رسالة نجاح
  ✅ تحديث البيانات من السيرفر
```

**الخطوات:**
1. عدّل الاسم
2. اختر صورة جديدة
3. اضغط "حفظ"
4. انتظر النتيجة

**النتيجة المتوقعة:** ✅ الاسم والصورة يُحفظان معاً

---

### 🔹 اختبار 5: تعطيل الأزرار أثناء التحميل

```gherkin
Given: المستخدم ضغط "حفظ"
When: جاري التحميل
Then:
  ✅ زر "حفظ" معطل (IsEnabled = false)
  ✅ زر "تغيير الصورة" معطل
  ✅ ActivityIndicator يعمل
  ✅ لا يمكن الضغط على الأزرار مرتين
```

**الخطوات:**
1. أدخل بيانات
2. اضغط "حفظ"
3. حاول الضغط مرة أخرى قبل انتهاء التحميل
4. تحقق أن الضغط لا يعمل

**النتيجة المتوقعة:** ✅ الزر لا يستجيب (معطل)

---

### 🔹 اختبار 6: معالجة الأخطاء - بدون إنترنت

```gherkin
Given: المستخدم بدون إنترنت
When: يضغط "حفظ التغييرات"
Then:
  ✅ رسالة خطأ: "فشل تحديث البيانات"
  ✅ الأزرار تصبح مفعلة مجددًا
  ✅ في Console: أسباب الخطأ
```

**الخطوات:**
1. قطع الإنترنت (Airplane Mode)
2. اضغط "حفظ"
3. لاحظ رسالة الخطأ

**النتيجة المتوقعة:** ✅ رسالة خطأ واضحة

---

### 🔹 اختبار 7: معالجة الأخطاء - بيانات ناقصة

```gherkin
Given: صفحة تعديل البيانات
When: 
  - محو اسم المستخدم
  - الضغط "حفظ"
Then:
  ✅ رسالة: "يرجى إدخال الاسم"
  ✅ لا يُرسل الطلب
  ✅ الأزرار تبقى مفعلة
```

**الخطوات:**
1. امسح الاسم من TextField
2. اضغط "حفظ"
3. تحقق من الرسالة

**النتيجة المتوقعة:** ✅ رسالة تحذير

---

### 🔹 اختبار 8: الرجوع والعودة

```gherkin
Given: صفحة تعديل البيانات
When: 
  - عدّل البيانات
  - لم تحفظ التغييرات
  - اضغط زر العودة
Then:
  ✅ تعود للصفحة السابقة
  ✅ التغييرات لم تُحفظ
```

**الخطوات:**
1. عدّل الاسم
2. اضغط زر العودة (بدون حفظ)
3. عُد للصفحة مرة أخرى

**النتيجة المتوقعة:** ✅ الاسم عاد للقيمة الأصلية

---

## 🔬 اختبارات في Console

### ✅ اختبار 1: التحقق من الـ Logging

```csharp
// عند فتح الصفحة:
✅ User loaded: أحمد محمد

// عند الضغط "حفظ":
🔄 Starting user update...
📸 Adding image: /path/to/image.jpg
📡 Sending request to: https://test.center-yazan.com/api/users/profile/update
✅ User updated successfully
✅ User loaded: محمد أحمد (الاسم الجديد)
```

### ✅ اختبار 2: التحقق من الأخطاء

```csharp
// عند فشل الاتصال:
❌ Error loading user: Unable to connect to the remote server
❌ Update failed: InternalServerError
❌ Exception during user update: Connection timeout
```

---

## 📊 جدول الاختبارات

| # | الاختبار | الحالة | الملاحظات |
|---|---------|-------|---------|
| 1 | فتح الصفحة | ✅ يعمل | بيانات تُحمل صحيحة |
| 2 | تغيير الصورة | ✅ يعمل | معاينة فورية |
| 3 | تحديث الاسم | ✅ يعمل | بدون صورة |
| 4 | تحديث الاسم+الصورة | ✅ يعمل | معاً |
| 5 | تعطيل الأزرار | ✅ يعمل | أثناء التحميل |
| 6 | خطأ إنترنت | ✅ يعمل | رسالة خطأ |
| 7 | بيانات ناقصة | ✅ يعمل | تحقق من الإدخال |
| 8 | الرجوع | ✅ يعمل | بدون حفظ |

---

## 🚨 الأخطاء المحتملة والحل

### ❌ الخطأ: Binding غير صحيح
```
Microsoft.Maui.Controls.Xaml.Diagnostics.BindingDiagnostics: Warning: 
'UpdateUserCommand' property not found
```
**الحل:** ✅ تحقق من استخدام `AppViewModel.Instance`

---

### ❌ الخطأ: الصورة لا تُحفظ
```csharp
// فحص في UpdateUserInfo():
if (string.IsNullOrWhiteSpace(SelectedImagePath) || !File.Exists(SelectedImagePath))
{
    // الصورة لم تُختر
}
```
**الحل:** ✅ تحقق من `SelectedImagePath`

---

### ❌ الخطأ: الأزرار لا تُعطّل
```xaml
<!-- تحقق من Converter -->
IsEnabled="{Binding IsLoadUser, Converter={StaticResource InverseBoolConverter}}"
```
**الحل:** ✅ تأكد من وجود `InverseBoolConverter` في Resources

---

### ❌ الخطأ: بيانات قديمة تظهر
```csharp
// لم يتم استدعاء LoadUser() بعد التحديث
```
**الحل:** ✅ أضف `await LoadUser();` بعد النجاح

---

## ✅ قائمة التحقق النهائية قبل الإطلاق

- [ ] جميع الاختبارات اليدوية نجحت
- [ ] لا توجد تحذيرات في Console
- [ ] الـ Logging واضح ومفيد
- [ ] معالجة الأخطاء شاملة
- [ ] الصور تُرفع بنجاح
- [ ] البيانات تُحدث من السيرفر
- [ ] الأزرار تُعطّل/تُفعّل صحيحة
- [ ] رسائل النجاح/الفشل واضحة
- [ ] البناء نجح بدون أخطاء
- [ ] التطبيق يعمل على جميع الأجهزة

---

## 📝 ملاحظات التطوير

```csharp
// للتطوير والاختبار:
#if DEBUG
    Console.WriteLine("🔍 DEBUG: Data = " + JsonSerializer.Serialize(data));
#endif

// لتسريع الاختبار:
// قلل timeout الـ API للحصول على الأخطاء بسرعة
using var cts = new CancellationTokenSource(TimeSpan.FromSeconds(5));
```

---

**الحالة**: ✅ اختبارات شاملة جاهزة
**الإصدار**: 1.0
**آخر تحديث**: 2025-03-26
